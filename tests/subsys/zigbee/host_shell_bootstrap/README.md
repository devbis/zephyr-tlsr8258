# Zigbee Shell Bootstrap Host Test

This host test exercises the `samples/zigbee/zigbee_shell` commissioning guards
without booting Zephyr or requiring hardware.

Run from the Zephyr tree:

```sh
tests/subsys/zigbee/host_shell_bootstrap/run.sh
```
